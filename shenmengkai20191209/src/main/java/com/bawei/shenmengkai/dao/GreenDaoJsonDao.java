package com.bawei.shenmengkai.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bawei.shenmengkai.bean.GreenDaoJson;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GREEN_DAO_JSON".
*/
public class GreenDaoJsonDao extends AbstractDao<GreenDaoJson, Void> {

    public static final String TABLENAME = "GREEN_DAO_JSON";

    /**
     * Properties of entity GreenDaoJson.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property JsonStr = new Property(0, String.class, "jsonStr", false, "JSON_STR");
    }


    public GreenDaoJsonDao(DaoConfig config) {
        super(config);
    }
    
    public GreenDaoJsonDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GREEN_DAO_JSON\" (" + //
                "\"JSON_STR\" TEXT);"); // 0: jsonStr
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GREEN_DAO_JSON\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GreenDaoJson entity) {
        stmt.clearBindings();
 
        String jsonStr = entity.getJsonStr();
        if (jsonStr != null) {
            stmt.bindString(1, jsonStr);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GreenDaoJson entity) {
        stmt.clearBindings();
 
        String jsonStr = entity.getJsonStr();
        if (jsonStr != null) {
            stmt.bindString(1, jsonStr);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public GreenDaoJson readEntity(Cursor cursor, int offset) {
        GreenDaoJson entity = new GreenDaoJson( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0) // jsonStr
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GreenDaoJson entity, int offset) {
        entity.setJsonStr(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(GreenDaoJson entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(GreenDaoJson entity) {
        return null;
    }

    @Override
    public boolean hasKey(GreenDaoJson entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
